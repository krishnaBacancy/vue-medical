name: vuejs-medical-CICD
on:
  push:
    branches: master
    
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v1
      - name: yarn install
        run: yarn install
      - name: pm2
        run: pm2 list
      - name: Deploy application
        run: pm2 restart 0
